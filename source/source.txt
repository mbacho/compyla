testf12xn(){
  if (right <= 4) then {
    men = "wom#en" + 'children';
  } else if (now == thn) then {
    opinion = ails + bils;
  } else {
    cats = dogs + cows;
  }
  if (cats < dogs) then {
    cows = rats + mice;
  }
}
#include<source_oneline.txt>
#define MAXI 3*4
#MAXI